p There are {{posts.length}} posts
div(ng-repeat='post in posts' ,ng-animate="'animate'")
  h3 {{post.title}}
  div {{post.text}}
  a(href='/readPost/{{post.id}}') More
  |  - 
  a(href='/editPost/{{post.id}}') Edit
  |  - 
  a(href='/deletePost/{{post.id}}') Delete